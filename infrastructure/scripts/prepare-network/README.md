# prepare-network

Prepares a single K8s node's network configuration for cluster use. This script must be run on **each of the 3 nodes** before running the cluster-wide verification.

## Usage

```bash
sudo ./prepare-network.sh
